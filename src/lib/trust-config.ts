// ---------------------------------------------------------------------------
// Доверителни съобщения (trust / urgency / authority) от CRO одита.
//
// ВАЖНО: тук НЕ слагаме измислени цифри. Всяко поле, което представлява
// конкретен факт за бизнеса (краен час за изпращане същия ден, брой клиенти,
// години на пазара), е празно по подразбиране и просто не се показва на
// сайта, докато не въведеш истинската стойност тук. Това пази сайта от
// подвеждащи твърдения пред клиенти.
// ---------------------------------------------------------------------------

export const TRUST_CONFIG = {
  // Час, до който поръчките се обработват и изпращат СЪЩИЯ ден.
  // Пример: "16:00". Остави "" ако не искаш да показваш това съобщение.
  sameDayCutoffTime: "16:00",

  // Кратко "authority" съобщение - реален брой клиенти / поръчки.
  // Пример: "Над 5000 доволни клиенти"
  customersServedText: "Над 25 000 доволни клиента",

  // Кратко "authority" съобщение - реален стаж на бизнеса.
  // Пример: "10 години опит в мъжката мода"
  yearsInBusinessText: "",

  // Срок за връщане/замяна (в дни). 14 е законовият минимум в ЕС за
  // онлайн поръчки - използваме него като безопасна база, докато не потвърдиш
  // друг (напр. 30 дни), ако магазинът предлага по-дълъг срок доброволно.
  returnWindowDays: 14,

  // Реални социални мрежи на бизнеса - взети директно от живия topstyle.bg
  // (footer + header), не измислени.
  facebookUrl: "https://www.facebook.com/topstyle.bg/",
  instagramUrl: "https://www.instagram.com/topstyle.bg/?hl=bg",

  // Имейл за контакт - остави "" ако не искаш да се показва (на оригиналния
  // сайт е скрит зад Cloudflare email-обфускация, така че не можем да го
  // прочетем автоматично - въведи го тук, ако искаш да излиза във футъра).
  contactEmail: "",

  // Viber номер - на оригиналния сайт е същия като телефона.
  viberPhone: "0877 968 927",
} as const;
